{{- if .Values.registry.enabled }}
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: {{ .Values.registry.imageStreamName }}
  labels:
    {{- include "zenml-stack.labels" . | nindent 4 }}
spec: {}
{{- end }}
