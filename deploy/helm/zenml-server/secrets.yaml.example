# Copy to secrets.yaml beside this file, restrict permissions with `chmod 600`,
# and set any passwords you want to pin explicitly.
#
#   cp secrets.yaml.example secrets.yaml
#
# Leave password fields empty on first install to auto-generate credentials.
# On later upgrades, existing cluster Secrets are preserved even when these
# fields stay empty.

database:
  password: ""
  rootPassword: ""
